<div class="navbar-outer">
  <div class="navbar-title">Authentication Form</div>
  <div class="user-welcome-navbar-title">
    Welcome <%= logged_in_user %> <% if(isAuthenticated){ %>
    <form method="POST" action="/logout">
      <%- include("./csrf-token-input.ejs") %>
      <button>logout</button>
    </form>
    <% } %>
  </div>
</div>
